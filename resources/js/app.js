// Cropper.js v2 — CSS-in-JS, no separate CSS import needed.
// Importing auto-defines all custom elements (<cropper-canvas>, <cropper-image>, etc.).
import Cropper from 'cropperjs';

// Expose constructor for Alpine/Livewire access (e.g. new Cropper(element, options))
window.Cropper = Cropper;

